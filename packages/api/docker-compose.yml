version: "3.0"

services:
  api_db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: api
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  redis_db:
    image: redis
    ports:
      - "6379:6379"
    volumes:
      - ./redisdata:/data
